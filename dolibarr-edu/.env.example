# ─────────────────────────────────────────────────────────────────────────────
#  DOLIBARR EDU — Variables de entorno
#  Copia este fichero como .env y rellena los valores reales
# ─────────────────────────────────────────────────────────────────────────────

# ── Base de datos Dolibarr (MariaDB) ─────────────────────────────────────────
MYSQL_ROOT_PASSWORD=cambia_esta_contrasena_root
MYSQL_DATABASE=dolibarr
MYSQL_USER=dolibarr
MYSQL_PASSWORD=cambia_esta_contrasena_db

# ── Dolibarr ERP ──────────────────────────────────────────────────────────────
# URL pública (la de Cloudflare, ej: https://erp.micentro.es)
DOLI_URL_ROOT=https://erp.micentro.es
DOLI_DOMAIN=erp.micentro.es

# Cuenta de superadministrador (solo para instalación inicial)
DOLI_ADMIN_LOGIN=admin
DOLI_ADMIN_PASSWORD=cambia_esta_contrasena_admin
DOLI_ADMIN_EMAIL=admin@micentro.es

# Nombre del centro educativo (aparece en la cabecera del ERP)
DOLI_COMPANY_NAME=Centro FP Administración de Empresas

# Puerto local de Dolibarr (NO exponer al exterior; Cloudflare hace el túnel)
APP_PORT=8069

# ── OpenProject — Gestión de proyectos ───────────────────────────────────────
# URL pública de OpenProject
OP_HOST=proyectos.micentro.es

# Base de datos exclusiva de OpenProject (PostgreSQL)
OP_DB_PASSWORD=cambia_esta_contrasena_openproject

# Clave secreta interna de Rails (genera una con: openssl rand -hex 64)
OP_SECRET_KEY=cambia_esta_clave_secreta_openproject

# Puerto local de OpenProject
OP_PORT=8070

# ── Collabora Online — LibreOffice en el navegador ───────────────────────────
# Usuario y contraseña del panel de administración de Collabora
COLLABORA_ADMIN=admin
COLLABORA_PASSWORD=cambia_esta_contrasena_collabora

# Puerto local de Collabora
COLLABORA_PORT=9980

# ── Panel de Gestión (Node.js) ────────────────────────────────────────────────
# URL pública del panel (ej: https://panel.micentro.es)
PANEL_URL=https://panel.micentro.es

# Base de datos exclusiva del panel (PostgreSQL)
PANEL_DB_PASSWORD=cambia_esta_contrasena_panel

# URL de la API de Dolibarr (igual que DOLI_URL_ROOT)
DOLIBARR_BASE_URL=https://erp.micentro.es

# Contraseña del panel (hash SHA-256 — se genera en el instalador)
ADMIN_PASSWORD_HASH=

# Clave de sesión (genera con: openssl rand -base64 48)
SESSION_SECRET=cambia_esta_clave_sesion

# Puerto local del panel web
PANEL_PORT=8068

# ── Cloudflare Tunnel ─────────────────────────────────────────────────────────
CLOUDFLARE_TOKEN=
